const DATE=/^\d{4}-\d{2}-\d{2}$/;
export const dateInIndia=()=>new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Kolkata",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());
export const buildHomeSlots=({provider,fromDate=dateInIndia(),days=7,bookedStarts=new Set()})=>{if(!DATE.test(fromDate))return[];const base=new Date(`${fromDate}T00:00:00+05:30`);const now=Date.now();const slots=[];for(let offset=0;offset<days;offset+=1){const day=new Date(base.getTime()+offset*86400000);const date=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Kolkata",year:"numeric",month:"2-digit",day:"2-digit"}).format(day);const localWeekday=new Date(`${date}T12:00:00Z`).getUTCDay();if(!provider.workingDays.includes(localWeekday))continue;for(const hour of provider.slotHours){const start=new Date(`${date}T${String(hour).padStart(2,"0")}:00:00+05:30`);if(start.getTime()<=now||bookedStarts.has(start.getTime()))continue;slots.push({start:start.toISOString(),end:new Date(start.getTime()+provider.visitMinutes*60000).toISOString()});}}return slots;};
export const findHomeSlot=({provider,startTime})=>{const requested=new Date(startTime);if(Number.isNaN(requested.getTime()))return null;const from=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Kolkata",year:"numeric",month:"2-digit",day:"2-digit"}).format(requested);return buildHomeSlots({provider,fromDate:from,days:1}).find((slot)=>new Date(slot.start).getTime()===requested.getTime())||null;};
