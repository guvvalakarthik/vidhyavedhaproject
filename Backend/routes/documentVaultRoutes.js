import express from"express";import multer from"multer";import{downloadDocument,listMyDocuments,revokeDocument,uploadDocument}from"../controllers/documentVaultController.js";import{protect}from"../middleware/authMiddleware.js";import{validateRequest}from"../middleware/validateRequest.js";import{vaultDocumentParamsSchema,vaultMetadataSchema}from"../validation/schemas.js";
const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024,files:1},fileFilter:(_q,file,cb)=>cb(null,["application/pdf","image/jpeg","image/png"].includes(file.mimetype))});const r=express.Router();r.use(protect);r.get("/",listMyDocuments);r.post("/",upload.single("document"),validateRequest({body:vaultMetadataSchema}),uploadDocument);r.get("/:documentId/download",validateRequest({params:vaultDocumentParamsSchema}),downloadDocument);r.delete("/:documentId",validateRequest({params:vaultDocumentParamsSchema}),revokeDocument);export default r;
