import{randomBytes}from"node:crypto";import{describe,expect,it}from"vitest";import VaultDocument from"../models/VaultDocument.js";import{decryptValue,encryptValue}from"../services/documentVaultService.js";import{vaultMetadataSchema}from"../validation/schemas.js";
describe("encrypted document vault",()=>{it("round-trips with AES-256-GCM and rejects tampering",()=>{const key=randomBytes(32),plain=Buffer.from("private evidence");const encrypted=encryptValue(plain,key);expect(decryptValue(encrypted,key)).toEqual(plain);encrypted.ciphertext[0]^=1;expect(()=>decryptValue(encrypted,key)).toThrow()});it("requires explicit consent and strips it",()=>{const result=vaultMetadataSchema.safeParse({label:"Income certificate",category:"income",consent:true});expect(result.success).toBe(true);expect(result.data.consent).toBeUndefined()});it("does not store file bytes or original filename in MongoDB",()=>{expect(VaultDocument.schema.path("file")).toBeUndefined();expect(VaultDocument.schema.path("originalName")).toBeUndefined();expect(VaultDocument.schema.path("storageName").options.select).toBe(false)})});
