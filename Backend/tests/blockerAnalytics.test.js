import{describe,expect,it}from"vitest";import BlockerEvent from"../models/BlockerEvent.js";import{analyticsActorHash}from"../services/blockerAnalyticsService.js";import{blockerEventSchema}from"../validation/schemas.js";describe("privacy-safe blocker analytics",()=>{it("uses a keyed pseudonym and no userId/free text fields",()=>{process.env.ANALYTICS_HASH_KEY="test-key-at-least-long-enough";expect(analyticsActorHash("user-a")).not.toBe(analyticsActorHash("user-b"));expect(BlockerEvent.schema.path("userId")).toBeUndefined();expect(BlockerEvent.schema.path("notes")).toBeUndefined();expect(BlockerEvent.schema.path("actorHash").options.select).toBe(false)});it("accepts taxonomy values and rejects arbitrary text",()=>{const event={serviceCode:"government",stage:"document-readiness",reason:"missing-document",route:"/readiness"};expect(blockerEventSchema.safeParse(event).success).toBe(true);expect(blockerEventSchema.safeParse({...event,notes:"private detail"}).success).toBe(false)});it("expires raw events and deduplicates daily reports",()=>{expect(BlockerEvent.schema.indexes().some(([x,o])=>x.actorHash===1&&o.unique)).toBe(true);expect(BlockerEvent.schema.path("expiresAt")).toBeDefined()})});
