import {describe,expect,it} from "vitest";import StatusTracker from "../models/StatusTracker.js";import {STATUS_PROVIDERS} from "../data/statusProviders.js";import {readProviderStatus,registerStatusConnector} from "../services/statusConnectorService.js";
describe("status connector registry",()=>{it("labels unconfigured external providers honestly",async()=>{const result=await readProviderStatus({providerCode:"external-government",targetId:"REF-1",userId:"user"});expect(result.connector).toBe("not-connected");expect(result.currentStatus).toBe("not-connected");expect(result.statusMessage).toMatch(/No verified official API/i);});it("accepts a reviewed official adapter without changing the tracker contract",async()=>{registerStatusConnector("external-government",async()=>({currentStatus:"under-review",statusMessage:"Returned by test adapter.",sourceUpdatedAt:new Date()}));const result=await readProviderStatus({providerCode:"external-government",targetId:"REF-1",userId:"user"});expect(result.connector).toBe("official-api");expect(result.currentStatus).toBe("under-review");});it("covers all nine journeys and stores owner scope",()=>{expect(STATUS_PROVIDERS).toHaveLength(9);expect(StatusTracker.schema.path("userId").options.required).toBe(true);});});
