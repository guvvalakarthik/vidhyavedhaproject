import React,{useEffect,useState}from"react";import api from"../services/api.js";import"./BlockerAnalytics.css";
function BlockerAnalytics(){const[data,setData]=useState(null),[days,setDays]=useState(30);useEffect(()=>{api.get("/blockers/report",{params:{days}}).then(x=>setData(x.data))},[days]);return <main className="blocker-page"><header><div><p>Thresholded service analytics</p><h1>Where residents report getting blocked</h1><span>{data?.privacyBoundary||"Loading privacy-safe groups..."}</span></div></header><div className="blocker-report"><label>Window<select value={days} onChange={e=>setDays(Number(e.target.value))}><option value="7">7 days</option><option value="30">30 days</option><option value="90">90 days</option></select></label><p>Groups appear only after at least {data?.minimumGroupSize||3} distinct daily pseudonymous signals.</p>{data?.groups.length===0?<p>No group has reached the privacy threshold.</p>:data?.groups.map(x=><article key={`${x.serviceCode}-${x.stage}-${x.reason}`}><strong>{x.count}</strong><div><h2>{x.serviceCode} · {x.stage}</h2><p>{x.reason}</p></div></article>)}</div></main>}export default BlockerAnalytics;
