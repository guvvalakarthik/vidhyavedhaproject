import React,{useEffect,useState}from"react";import api from"../services/api.js";function AssistedHandoff(){const[c,setC]=useState([]),[items,setItems]=useState([]),[f,setF]=useState({centreCode:"",serviceCode:"government",supportMode:"phone",preferredLanguage:"English",phone:"",summary:"",consent:false}),[note,setNote]=useState("");useEffect(()=>{Promise.all([api.get("/handoffs/centres"),api.get("/handoffs/mine")]).then(([a,b])=>{setC(a.data.centres);setF(x=>({...x,centreCode:a.data.centres[0]?.centreCode||""}));setItems(b.data.handoffs)})},[]);const ch=e=>setF(x=>({...x,[e.target.name]:e.target.type==="checkbox"?e.target.checked:e.target.value}));const go=async e=>{e.preventDefault();const{data}=await api.post("/handoffs",f);setItems(x=>[data.handoff,...x]);setNote(data.message)};return <main className="tracking-page"><header><div><p>Verified human help</p><h1>Ask an assisted-service desk to guide you</h1><span>Consent is required before contact. The desk guides preparation; authorities still decide official services.</span></div></header>{note&&<p className="tracking-note">{note}</p>}<div className="tracking-grid"><form onSubmit={go}><h2>Request handoff</h2><label>Verified centre<select name="centreCode" value={f.centreCode} onChange={ch}>{c.map(x=><option value={x.centreCode} key={x.centreCode}>{x.name}</option>)}</select></label><label>Phone<input name="phone" value={f.phone} onChange={ch} required/></label><label>What help is needed?<input name="summary" value={f.summary} onChange={ch} required minLength="10"/></label><label><input type="checkbox" name="consent" checked={f.consent} onChange={ch} required/> I consent to contact by this verified desk.</label><button>Request assistance</button></form><section><h2>My handoffs</h2>{items.map(x=><article key={x.handoffId}><span>{x.handoffId} · {x.status}</span><h3>{x.serviceCode}</h3><p>{x.summary}</p></article>)}</section></div></main>}export default AssistedHandoff;
